1. Install <a href="https://docs.docker.com/desktop/#download-and-install">Docker</a> on your system.

2. Go to the terminal of this directory and run "docker-compose up -d --build"

3. Once the container is up and running, you can access the app at: <a href="http://localhost:5000/">localhost:5000</a>